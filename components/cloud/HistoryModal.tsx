// Temporary stub for Task 10 so the app compiles. Task 11 replaces this with the real
// version history modal implementation.
export function HistoryModal({ cloudProjectId, onRestore, onClose }: { cloudProjectId: string; onRestore: (state: any) => void; onClose: () => void }) {
    return null;
}
