export function renderCode(el, ctx) {
    el.innerHTML = '<section class="pane" style="flex:1"><div class="pane-title">code</div>' +
        '<div class="pane-body"><p class="stub">coming soon — filled by a later task</p></div></section>';
}
